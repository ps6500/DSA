"""
leetcode 219:

Given an integer array nums and an integer k, return true if there are two distinct indices i and j in the array such that nums[i] == nums[j] and abs(i - j) <= k.

 

Example 1:

Input: nums = [1,2,3,1], k = 3
Output: true
Example 2:

Input: nums = [1,0,1,1], k = 1
Output: true
Example 3:

Input: nums = [1,2,3,1,2,3], k = 2
Output: false
 

Constraints:

1 <= nums.length <= 105
-109 <= nums[i] <= 109
0 <= k <= 105


def containsNearbyDuplicate(nums: List[int], k: int) -> bool:
    hash = {}
    left = 0
    right = 0
    for index, num in enumerate(nums):
        if num in hash:
            for index_val in hash[num]:
                if abs(index_val - index) <= k:
                    return True
        else:
            hash[num] = []
        hash[num].append(index)
    return False

"""

def containsNearbyDuplicate(nums: List[int], k: int) -> bool:
    index_map = {}

    for i, num in enumerate(nums):
        # Check if the number is already in the map
        # and if the difference between the current index and the stored index is <= k
        if num in index_map and i - index_map[num] <= k:
            return True
        
        # Update the map with the current index of the number
        index_map[num] = i
        print(index_map)
        
    # If no such pair is found after the entire array is traversed
    return False

if __name__ == '__main__':
    nums = [1,2,3,1]
    k = 3
    print(containsNearbyDuplicate(nums, k))